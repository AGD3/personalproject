//Import actions that apply
// Further Review
import axios from 'axios';

const toLowerCase = (text) => text.toLowerCase()

export default function getSearchInput(input) {
    //Check for string input

    //This is where you'll dispatch imported actions
}